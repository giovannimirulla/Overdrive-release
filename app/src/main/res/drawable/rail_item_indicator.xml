<?xml version="1.0" encoding="utf-8"?>
<!--
  Selected/unselected pill behind the rail-item icon.

  Selected state uses colorSurfaceContainerHighest (neutral, no brand
  green) with a thin colorOutlineVariant outline so the pill reads as a
  defined surface even on dark theme where flat tonal fills can disappear
  into the rail background.

  Pressed state uses colorSurfaceContainerHigh (one tier lower than the
  rail's idle surface) so the press is visible without flashing brand
  color.
-->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_selected="true">
        <shape android:shape="rectangle">
            <solid android:color="?attr/colorSurfaceContainerHighest" />
            <stroke android:width="1dp" android:color="?attr/colorOutlineVariant" />
            <corners android:radius="16dp" />
        </shape>
    </item>
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="?attr/colorSurfaceContainerHigh" />
            <corners android:radius="16dp" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <corners android:radius="16dp" />
        </shape>
    </item>
</selector>
